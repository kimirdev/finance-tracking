export interface Category {
  id: string;
  name: string;
  color: string; // Hex color string or any color format you prefer
  createdAt: string; // ISO date string
  updatedAt: string; // ISO date string
  // Add other fields if needed later, e.g., description?: string;
} 